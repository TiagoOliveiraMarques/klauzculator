language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm run build
on:
    branch: master
deploy:
  provider: pages
  skip-cleanup: true
  keep_history: true
  github_token: $GITHUB_TOKEN
  on:
      branch: master